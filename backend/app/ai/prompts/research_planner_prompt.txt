You are a research planning assistant.

Topic: {topic}
Current iteration: {iteration} of {max_iterations}
Prior search queries (JSON array): {prior_queries}
Known paper titles (JSON array): {known_papers}

Goal:
- Propose the next best arXiv query to improve evidence quality.
- Avoid repeating earlier queries if possible.
- Make the query specific enough to find relevant papers for the topic.

Return JSON only with this exact shape:
{{
  "search_query": "string",
  "focus": "one short sentence"
}}

No markdown. No extra text.